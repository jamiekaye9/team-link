<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workers</title>
</head>
<body>
    <h1>Workers</h1>
    <a href="/company/add-worker">Add Worker</a>
    <h2><%= companyName %></h2>
    <ul>
        <% workers.forEach(worker => { %>
            <li><%= worker.name %> - <%= worker.position %></li>
        <% }) %>
    </ul>
</body>
</html>